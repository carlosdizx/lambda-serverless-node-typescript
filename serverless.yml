service: aws-lambda-node-crud
frameworkVersion: '3'

plugins:
  - serverless-offline
  - serverless-plugin-typescript

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-2

functions:
  api:
    handler: index.handler
    events:
      - httpApi:
          path: /
          method: get
